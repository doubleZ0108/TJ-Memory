window.onload = function(){
    initNavigation();
   
    initHero();

    initAwesomeStructure();
    initLogic(true);

    initWaveSection();

    // initHistory();
    initEmptyHistory();

    initShare();
    initSocialMedia();
};
